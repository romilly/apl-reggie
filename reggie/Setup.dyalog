 Setup
 dash←'-'
 slash←'/'
 colon←':'
 digit←'\d'
 digits←'\d+'
 osp←'(\s)?'
 space←'\s'
 capital←'[A-Z]'
 capitals←'[A-Z]+'
 character←'.'
 plus←'\+'
 letter←'[A-Za-z]'
 an←'[A-Za-z0-9]'
 lp←'\('
 rp←'\)'
